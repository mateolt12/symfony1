{# templates/produtos/new.html.twig #}
{% form_theme form 'bootstrap_3_layout.html.twig' %}
{#{% extends 'produtos/produtos.html.twig'%}#}
{%block content%}
        <div class="row">
            <div class="col-lg-12">
                        <h1 class="page-header">
                                Novo Produto
                        </h1>
            </div>
                    <!-- /.col-lg-12 -->
        </div>
        <div class="row">
            <div class="col-lg-12">
            <div class="col-lg-6">
				<div class="card-body">
					<div class="row">
					{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
					{{ form_errors(form) }}
			
			
					{{ form_end(form) }}					

					</div>        
				</div>
			</div>
            </div>
        </div>
{%endblock%}